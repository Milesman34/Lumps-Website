// Default configs
export const defaultConfigs = {
    // Clean slate configs
    cleanSlate: {
        // Is clean slate enabled?
        enabled: false,

        // How many clean slates can you do per turn?
        perTurn: 1
    },

    // Desperation configs
    desperation: {
        // Is desperation enabled?
        enabled: false,

        // Can desperation be done more than once per game per player
        repeatable: false
    },

    // Is extra chance enabled?
    extraChance: false
}